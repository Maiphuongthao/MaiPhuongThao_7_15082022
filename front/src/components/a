<form v-if="contentChange" class="card-body">
          <input
            @change="onImageChange"
            name="imageUrl"
            type="file"
            class="form-control"
            id="formFile"
            ref="fileInput"
            hidden
          />
          <button type="file" class="btn btn-color mb-2">
            <div for="formFile">
              <i class="fas fa-images"></i>
            </div>
          </button>
          <button type="submit" class="btn btn-color mb-2 mx-2">
            <i class="far fa-paper-plane"></i>
          </button>
          <div class="form-group">
            <textarea
              class="form-control"
              id="message"
              rows="3"
              :placeholder="this.post.content"
              v-model="this.newPost.content"
            ></textarea>
          </div>
        </form>